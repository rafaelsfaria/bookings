{{template "base" .}}

{{define "content"}}
<div class="container">
  <div class="row">
    <div class="col">
      <h1>About page</h1>
      <p>about page text</p>
      <p>this come from the template: {{index .StringMap "test"}}</p>

      <p>
        {{if ne (index .StringMap "remote_ip") ""}}
          Your remote ip address is {{ index .StringMap "remote_ip" }}
        {{else}}
          I don't know yout ip address yet. Please visit the <a href="/">Home page</a> so I can see it.
        {{end}}
      </p>
    </div>
  </div>
</div>
{{end}}
